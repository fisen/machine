/**
* @Author Magnus Larsson, Marco Dondio
**/

function CanvasManager() {
	
};

CanvasManager.prototype.translateWorkSpace = function() {
	
};

CanvasManager.prototype.addCogWheel = function(cogWheel) {
	
};


CanvasManager.prototype.leftMouseButtonPressed = function() { //Check wheel, save cog wheel reference, save (x,y) translation from cog center
	
};

CanvasManager.prototype.leftMouseButtonReleased = function() { //delete cog wheel reference (null) (and cog wheel translation)
	
};

CanvasManager.prototype.dragTo = function(newXPos, newYPos) { //If cog wheel is selected, move cog wheel to new pos with regards to selected translation of cog and the new position
															//If no cog wheel is selected translate canvas workspace
	
};

CanvasManager.prototype.rightMouseButtonClick = function() { //Check for wheel, -> popup menu
	
};






//Right click on the canvas will open a popup menu (if u r clicking on a wheel)

//When translating canvas workspace translate the background for maybe 1/4 of the same amount

